# 리허설시티 Python 의존성 (Python 3.12)
# 새 의존성 추가 시 이 파일 갱신 필수 (CLAUDE.md 개발 규칙)
# 설치:  pip install -r requirements.txt

# --- 설정 / 환경 ---
python-dotenv>=1.0,<2.0
pydantic>=2.7,<3.0
pydantic-settings>=2.3,<3.0

# --- 데이터 수집 (data/) ---
requests>=2.32,<3.0

# --- 지리 · 시뮬레이션 코어 (engine/) ---
numpy>=1.26,<3.0
pandas>=2.2,<3.0
shapely>=2.0,<3.0
geopandas>=1.0,<2.0
osmnx>=2.0,<3.0
networkx>=3.3,<4.0

# --- 데이터베이스 (PostgreSQL + PostGIS) ---
psycopg[binary]>=3.2,<4.0
SQLAlchemy>=2.0,<3.0
GeoAlchemy2>=0.15,<1.0

# --- API 서버 (api/) ---
fastapi>=0.115,<1.0
uvicorn[standard]>=0.30,<1.0
websockets>=12.0

# --- LLM (Phase 3 전까지 미사용 — CLAUDE.md 원칙 5 / DECISIONS D1) ---
anthropic>=0.40,<1.0

# --- 개발 도구 ---
pytest>=8.2,<9.0
ruff>=0.5,<1.0
